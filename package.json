{
  "name": "orbis-pictus",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run typecheck --workspace=packages/shared && npm run typecheck --workspace=apps/server && npm run build --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=packages/shared && npm run typecheck --workspace=apps/server && npm run typecheck --workspace=apps/web",
    "test": "npm run test --workspace=packages/shared && npm run test --workspace=apps/server",
    "db:migrate": "npm run db:migrate --workspace=apps/server"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
